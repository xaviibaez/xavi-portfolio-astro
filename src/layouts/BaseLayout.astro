---
import Nav from '@/components/Nav.astro';
import type { Lang } from '@/i18n/utils';
import { getOgLocale } from '@/i18n/utils';
import { ClientRouter } from 'astro:transitions';
import '@/styles/global.css';

interface Props {
  lang: Lang;
  title?: string;
  description?: string;
}

const {
  lang,
  title = 'Xavi Ibáñez — Senior Backend Engineer',
  description = 'Portfolio of Xavi Ibáñez Martínez, Senior Backend Engineer at BBVA Technology. Microservices, cloud-native systems, Java, TypeScript.',
} = Astro.props;

const canonicalURL = Astro.url.href;
const ogLocale = getOgLocale(lang);

const jsonLd = JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Xavi Ibáñez Martínez',
  jobTitle: 'Senior Backend Engineer',
  url: 'https://xavi.dev',
  sameAs: [
    'https://github.com/xaviibaez',
    'https://www.linkedin.com/in/xavi-ib%C3%A1%C3%B1ez-mart%C3%ADnez-09a7b415a/',
  ],
  worksFor: { '@type': 'Organization', name: 'BBVA Technology' },
});
---

<!doctype html>
<html lang={lang} data-theme="light">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/profilePic.webp" />
    <title>{title}</title>

    <ClientRouter />

    <!-- Prevent flash of wrong theme -->
    <script is:inline src="/theme-init.js"></script>
    <!-- Theme toggle (event delegation persists across View Transitions) -->
    <script is:inline>
      document.addEventListener('click', function (e) {
        var btn = e.target.closest('[data-toggle-theme]');
        if (!btn) return;
        var current = document.documentElement.getAttribute('data-theme') || 'light';
        var themes = btn.getAttribute('data-toggle-theme').split(',');
        var next = themes.find(function (t) { return t.trim() !== current; }) || themes[0].trim();
        document.documentElement.setAttribute('data-theme', next);
        localStorage.setItem('theme', next);
      });
      // Re-apply saved theme after each page swap
      document.addEventListener('astro:after-swap', function () {
        var theme = localStorage.getItem('theme') || 'light';
        document.documentElement.setAttribute('data-theme', theme);
      });
    </script>

    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:locale" content={ogLocale} />
    <meta property="og:site_name" content="xavi.dev" />

    <!-- Structured data -->
    <Fragment set:html={`<script type="application/ld+json">${jsonLd}</script>`} />
  </head>
  <body>
    <Nav lang={lang} />
    <main class="w-full min-h-[calc(100vh-3.5rem-60px)]">
      <slot />
    </main>
    <footer class="border-t border-border px-6 py-5 mt-16">
      <div class="max-w-[1200px] mx-auto text-center">
        <p class="text-muted text-[0.85rem]">
          © {new Date().getFullYear()} Xavi Ibáñez Martínez · Built with
          <a href="https://astro.build" class="text-accent no-underline">Astro</a>
        </p>
      </div>
    </footer>
  </body>
</html>
